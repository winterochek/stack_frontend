import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => {
  return {
    iconBlock: {
      paddingRight: '37px',
    },
    themeIcon: {
      marginRight: '45px !important',
    },
  };
});
